#!/usr/bin/env php
<?php
	require __DIR__ . '/../src/client/utils/StreamToFile.php';
	
	$from = fopen('php://stdin', 'r');
	$to = fopen('php://stdout', 'w');
	$minPause = 1000;
	$maxPause = 10000;
	
	//$util -> new StreamToFile();
	//$util->stream($from,$to,$minPause,$maxPause);
	